language: node_js

node_js:
  - "8"

dist: trusty

sudo: required

cache:
  directories:
    - node_modules

addons:
  chrome: stable

before_script:
  - npm install
  - npm install -g @angular/cli
  - npm remove webpack -g
  - npm i webpack --save-dev

script:
  - npm run test
